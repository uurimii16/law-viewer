# 법령 조합 뷰어 — 작업·배포 전 과정 기록

작성일: 2026-07-02
저장소: https://github.com/uurimii16/law-viewer (public)
배포: https://law-viewer-rrpxeqva5ndtnznhzj4gnt.streamlit.app/
로컬 소스: `C:\Users\PC\Desktop\법뷰어\` (git 저장소, remote origin 연결)

---

## 1. 목적

법제처 OPEN API로 법령을 실시간으로 불러와, 필요한 법을 골라 조합해서 보고, 조문 속 인용을 그 자리에서 펼쳐 확인하며, PDF·HTML로 추출하거나 이용자 각자의 노션으로 푸시하는 웹앱.

기존 산출물의 대체:
- `D:\260623_법령집md_1650` : 법령집을 DOCX(3단/2단표)로 생성하는 파이프라인 (정적 산출물)
- `D:\노션재푸시` : 같은 데이터를 노션 페이지로 푸시하는 도구 (`push_guides.py`)

두 도구는 법 개정 시 다시 돌려야 하고 검색·조합이 불편 → 실시간·검색·조합 가능한 웹앱으로 전환.

---

## 2. 기술 선택

- **Streamlit + Streamlit Community Cloud** 채택.
  - 법제처 API는 브라우저 직접 호출(CORS)이 막혀 있고 HTTP → 순수 정적 사이트로는 실시간 호출 불가.
  - 서버(파이썬)가 대신 호출하는 Streamlit이면 CORS 문제 없음.
  - 기존 GIS 주소변환기(같은 계정 uurimii16, Streamlit Cloud)와 동일 패턴으로 배포 경험 재사용.
- **엔진 재사용**: `D:\노션재푸시\push_guides.py`의 법제처 호출·인용 해석 로직에서 노션 의존 부분만 걷어내 이식.
- **비용**: 앱은 Claude/AI를 사용하지 않음 → AI 토큰 소모 0(이용자 수 무관). 노션 토큰은 이용자 각자의 것. 법제처 OC(=uurimii16)는 무료(호출량 제한만 존재).

---

## 3. 파일 구성

```
법뷰어/
├─ streamlit_app.py         # UI 전체 (사이드바 선택·옵션, 4개 보기 모드, 내보내기, 노션 푸시)
├─ law_engine.py            # 법제처 호출 + 인용/위임/타법 해석 엔진 (노션·Streamlit 비의존)
├─ notion_sink.py           # 이용자별 노션 푸시 (토큰·부모페이지 인자화)
├─ exporters.py             # HTML / PDF 내보내기
├─ requirements.txt         # streamlit, reportlab, streamlit-sortables
├─ .streamlit/config.toml   # 테마(라이트, primaryColor #2E5AAC)
├─ .gitignore               # __pycache__, out/*.log, out/mother_cache.json, secrets
├─ .github/
│  ├─ keep_awake.py         # 절전방지: playwright로 앱 방문/깨우기
│  └─ workflows/keep-awake.yml  # 6시간마다 cron
└─ .devcontainer/           # (사용자가 GitHub에서 추가)
```

---

## 4. 법제처 API 사용

- 엔드포인트: `https://www.law.go.kr/DRF/lawSearch.do` (목록 검색), `.../lawService.do` (본문)
- 인증: `OC=uurimii16`, `type=JSON`, User-Agent 지정, 40초 타임아웃, 3회 재시도 (`drf()`)
- 법령 1건 = 검색(lawSearch) + 본문(lawService) **2콜** (`fetch_law_units`, `fetch_admrul`)
- 행정규칙(지침)은 `target=admrul`, 법률/시행령/시행규칙은 `target=law`

### 데이터 특이점 (실측으로 확인 후 대응)
- **호번호 필드가 6/6의2/6의3을 전부 `'6.'`로 반환** → 호가지(6의3)는 호내용 앞 토큰("6의3.")을 파싱해야 구분됨 (`_ho_struct`).
- **목(가·나·다)은 호번호 아래 별도 구조** → 명시적으로 담지 않으면 화면에서 누락됨.
- 조문내용·항내용 등이 문자열 또는 리스트로 옴 → `_s()`로 평탄화.
- 세목 (1) 2) 3)은 목내용 안에 `\n`으로 들어있음 → 마크다운 홑 줄바꿈 무시 문제로 표시 시 별도 처리 필요.

---

## 5. law_engine.py — 핵심 로직

### 호출/캐시
- `fetch_law_units(name)`, `fetch_admrul(name)` : 법제처 호출. **프로세스 캐시 `_FETCH`**로 같은 법령 중복 호출 방지.
- `clear_fetch_cache()` : 새로고침 시 캐시 비움.
- `search_law`, `search_admrul` : 목록 검색(검색 추가 UI 백엔드).

### 인용 해석 (자기 법령 = 법/영/규칙)
- `get_index(mother)` : 모법의 법/영/규칙 조문을 `_build_role_index`로 색인 `{role:{(조,가지):{title,head,full,항,호_flat}}}`.
- `make_cite(mother)` / `parse_targets(body)` : 본문 속 "법/영/규칙 제N조…" 탐지 및 나열·범위 전개.
- `resolve(idx, role, jo, gaji, hang, ho)` : 인용을 실제 조문 내용으로 해석. 못 찾으면 안내문.
- `line_cites(text, idx, cite)` : 한 줄 → [(라벨, [본문줄])] (📎 펼침용).

### 위임 관계 (법 ↔ 시행령·시행규칙)
- `_refs(text, who)` : 시행령/규칙 본문에서 "법 제N조…" 또는 "영 제M조…" 인용을 **(조, 가지, 항, 호, 호가지, 목) 6-tuple**로 추출.
  - `(?<![가-힣])` 가드로 건축법·기본법 등 꼬리 오탐 차단.
  - 인용 바로 뒤가 **"…에 따른 / …에 의한 ○○"(관형형=정의된 용어를 이름으로 지칭)** 이면 위임으로 보지 않고 제외 (`_ADNOM`). "…에 따라 / …에서 '○○'이란"(위임 실행·정의)은 유지.
- `build_delegation(mother)` : 법·시행령·시행규칙을 받아 역인덱스 생성.
  - `by_ref{(조,가지,항,호,호가지,목):{영[],규칙[]}}` : 인용한 **정확한 단위**별.
  - `by_jo`, `by_ho` : 조·호 단위 집계(호환).
  - 시행규칙이 "영 제M조"만 인용(2-hop)하면 그 영이 위임받은 법조를 찾아 조 단위로만 연결.
- `law_units_structured(units)` : 법을 조두문/항/호(호가지 포함)/목 구조로 분해 (위임·호단위 표시용).

### 타법 인용 (다른 법)
- `EXT_RE` / `external_cites_in_line(text)` : 「법령명」 제N조… 탐지.
- `resolve_external(lawname, body)` : 해당 법을 법제처에서 받아 조문 해석 (인덱스 `_EXT_IDX` 캐시).

### 내보내기/3단 모델
- `doc_sections(name, kind, mother, include_cites)` : HTML/PDF용 문서 모델.
- `three_column(name)` : 법·시행령·시행규칙을 조번호 기준으로 나란히(참고용).
- `render_units(units)` : 조 순서 유지, 항/호/목 포함한 줄 목록.

---

## 6. notion_sink.py — 이용자별 노션 푸시

- `push_guides.py`의 노션 로직 재사용, 토큰·부모페이지를 함수 인자로 받음(계정 하드코딩 없음).
- `napi(token, ...)` : 노션 API(속도제한 재시도).
- `blocks_for(doc, idx, cite)` : 문서를 노션 블록으로. 본문 인용은 그 자리 📎 토글로.
- `push_one(token, name, kind, parent, mother)` : 부모 페이지 아래 자식 페이지 생성(동명 페이지는 archive 후 재생성=멱등).
- `normalize_parent_id` : 노션 페이지 URL/32자리 ID → 대시 UUID.
- 토큰은 서버에 저장하지 않고 그 요청에만 사용.

---

## 7. exporters.py — 내보내기

- `build_html(sections, options)` : 자체완결 HTML(제목 색상, 인용 <details>, 배치 여백). 한글(HWP)·Word로도 열림.
- `build_pdf(sections, options)` : reportlab **내장 CID 폰트 `HYGothic-Medium`**으로 한글 렌더 → 외부 폰트 파일 불필요.
- 옵션: 인용 포함 여부, 제목 색상, 배치(기본/조밀).

---

## 8. streamlit_app.py — UI

### 법령 선택
- 기본 4개 법률(국계법·토지법·도정법·도시재생법)로 시작, **최대 10개**.
- 사이드바 "✏️ 법령 편집" 확장패널: 체크박스로 선택/해제 + 법령명 검색 추가(➕).
- **`streamlit-sortables`로 드래그(≡) 순서변경** (컴포넌트 없으면 ▲▼ 버튼 폴백).

### 보기 모드 4종
1. **탭 보기** : 선택한 법들을 탭으로.
2. **3단 비교(조번호 기준)** : 법·시행령·시행규칙을 조번호로 나란히(참고용).
3. **위임 3단** : 법을 조·항·호·목으로 나눠, 각 단위를 위임받은 시행령·시행규칙을 같은 행 3칼럼에 배치. 예) 제2조제20호 옆 영 제4조의3, 제6호의3가목 옆 영 제2조의2.
4. **호 단위 관련조문** : 각 호(예 13.공공시설) 아래 관련 시행령·규칙을 접이식으로.
   - 2~4번은 법 1개 선택 시 동작.

### 표시·추출 옵션
- 자기 법령 인용 펼침(📎), 타법 인용 펼침(📖, 느림), 제목 색상, 배치(기본/조밀), 본문 키워드 필터.

### 렌더 헬퍼
- `render_lines` : 줄 렌더 + 📎(자기법령)·📖(타법) 펼침.
- `law_html(text, level)` : 내부 `\n`을 `<br>`+들여쓰기로 렌더(호 1단계, 목 2단계, 세목 더 깊게). 이중번호 방지.
- `paginate(seq, key, per)` : 긴 목록을 페이지 분할(키워드 검색 중엔 전체).

### 내보내기 / 노션 (사이드바, 항상 보임)
- HTML 즉시 다운로드 / PDF는 "만들기" 버튼으로 생성 후 다운로드 / 노션 푸시(확장패널).

### 캐시
- `@st.cache_data`로 fetch·index·delegation·3dan·sections·external 결과를 rerun 간 재사용(TTL 6시간).
- "🔄 최신으로 새로고침" : `st.cache_data.clear()` + 엔진 `_FETCH`·`_IDX`·`_EXT_IDX` 비움.

---

## 9. 성능 최적화 (실측 기반)

측정(한국 기준, 미국 클라우드는 더 느림):
- 법령 1건 fetch ≈ 1.0~1.2초.
- `build_delegation` ≈ 3.2초(법+영+규칙 3콜), `get_index` ≈ 3.4초(같은 3개 **중복 fetch**), `three_column`도 동일 중복.
- 국계법 위임 3단 렌더 = **1230행**(각 행 = columns(3)+markdown+expander들).

조치:
1. **엔진 fetch 캐시 `_FETCH`** : 같은 법령을 여러 경로가 중복 호출하던 것 제거 → get_index·three_column 재fetch **3.4초 → 0초**, 모드 전환 즉시.
2. `_units_cached`의 디스크 캐시(mother_cache.json) 제거 → `_FETCH`로 일원화(새로고침 후 옛 데이터 물던 문제 해소).
3. **페이지네이션** : 위임3단(10조/쪽)·호단위·3단·탭을 조(줄) 단위 분할 → 렌더·상호작용 부담 대폭 감소.
결과: 첫 로딩만 ~3초(법제처 지연, 불가피), 이후 모드전환·rerun·페이지이동 즉시.

---

## 10. 배포 과정

1. 로컬 `법뷰어\` 파일 작성 → `python -m streamlit run streamlit_app.py`로 동작 확인.
2. 사용자가 github.com에서 빈 repo `uurimii16/law-viewer`(public) 생성.
3. `git init` → commit → `git remote add origin …` → `git push`(쓰기 자격증명 캐시됨, gh CLI 없음). git 사용자= name uurimii16 / email mdrqnx@gmail.com(repo-local).
4. share.streamlit.io → "Create app" → repo=`uurimii16/law-viewer`, branch=`main`, main file=`streamlit_app.py` → Deploy. → URL 발급.
5. 코드 수정은 로컬에서 고쳐 `git push`만 하면 자동 재배포. 반영 안 되면 앱 ⋮ → **Clear cache**(캐시만) 또는 Manage app → **Reboot app**(서버 재시작).

### 절전방지
- `.github/workflows/keep-awake.yml`(6시간 cron) + `.github/keep_awake.py`(playwright 방문/깨우기).
- URL은 repo Variable `STREAMLIT_URL`로 주입 — 미설정이면 조용히 건너뜀. **현재 미설정**(설정 시 Settings > Secrets and variables > Actions > Variables).

---

## 11. 커밋 이력

| 해시 | 시각 | 내용 |
|---|---|---|
| 5e6b9d7 | 14:57 | 법령 조합 뷰어 초기 커밋 |
| c3b64ea | 15:13 | 절전방지(keep-awake) GitHub Actions 추가 |
| 321c0e6 | 15:16 | 내보내기·노션 버튼을 사이드바로 이동(항상 보이게) |
| 8629831 | 15:33 | 보기 모드 3종 추가: 위임 3단, 호 단위 관련조문, 타법 인용 펼침 |
| 7c844cd | 15:42 | 위임 3단 3칸 표 + 법령 최대 10개 편집·드래그 순서변경 |
| 3829f57 | 15:48 | 위임 3단을 조·항·호 단위로 정밀 정렬 |
| 3d6dda5 | 15:55 | 목·호가지 인식 및 정밀 정렬 수정 |
| 5fa2c9d | 16:05 | 호/목 표시 정리: 이중번호 제거 + 세목 줄바꿈 들여쓰기 |
| adf5ada | 16:13 | Added Dev Container Folder (사용자) |
| adc0ada | 16:22 | 위임 오부착 수정: '에 따른/의한 ○○'은 위임에서 제외 |
| d6ffb02 | 16:37 | 성능 최적화: 중복 네트워크 제거 + 렌더 페이지네이션 |

---

## 12. 알려진 한계 / 다음 단계

- 시행령 조문의 약 17%(국계법 기준 84개 중 14개)는 「법명 전체」로 인용 → 현재 미부착. 전체이름 인용도 잡도록 보정 가능.
- 위임 판별은 "에 따른/의한" 제외 휴리스틱 → 드물게 "에 따른"이 진짜 위임인 경우 누락 가능.
- 3단(조번호 기준)은 조번호 우연 일치 기반이라 참고용.
- 첫 로딩 ~3초(법제처 지연): 앱 시작 시 기본 4법 선(先)로딩으로 체감 개선 가능.
- 페이지 크기 조절 UI, HWP(.hwp) 직접 출력은 미구현.

---

## 13. 실행 / 검증

```powershell
# 로컬 실행
cd C:\Users\PC\Desktop\법뷰어
python -m streamlit run streamlit_app.py

# 인용 해석 오프라인 확인
python law_engine.py --dry "지역·지구등의 지형도면 작성에 관한 지침"
```

- 위임/호 정렬 검증: 국계법 제2조제20호 → 영 제4조의3, 도시재생법 제2조제1항제6호의3가목 → 영 제2조의2.
- 오부착 검증: 도시재생법 영 제11조는 제9조제1항에만 붙고 제2조제6호나목엔 안 붙음.
- 규칙 호·목 표시 검증: 도시군계획시설규칙 제33조 목 가~자.
